# Maze width (number of cells)
WIDTH=36

# Maze height
HEIGHT=24

# Entry coordinates (x,y)
ENTRY=0,0

# Exit coordinates (x,y)
EXIT=24,18

# Output filename
OUTPUT_FILE=maze.txt

# Is the maze perfect?
PERFECT=True

# The seed to use (Optional)
# SEED=42

# Display mode (Optional)
# Valid = "ascii" (default), "betterascii" | "emoji"
DISPLAY=ascii

# Algorithm to use (Optional)
# Valid = "rb"(recursive backtracking(default))
# ALGORITHM=rb
